export enum ContentType {
  CONTENT_WORD_DEFINITION = 'content_word_definition',
  CONTENT_SENTENCES = 'content_sentences',
  QUESTION_AUDIO_IMAGE = 'question_audio_image',
  AUDIO_IMAGE = 'audio_image',
  UNKNOWN = 'unknown' // Fallback for unmapped types
}